# Design Tokens — SUPERSEDED

> This document previously listed token **names** with no values and is no longer
> authoritative. The concrete, canonical token definitions (colors, typography,
> spacing, radius, shadows, motion — with real values) now live in:
>
> **→ [ADQ Design System](./ADQ-Design-System.md) — the single source of truth.**
>
> Runtime values are defined in `app/src/styles/index.css` and documented in the
> canonical doc above. Update both together.
